<nav class="navbar navbar-expand-sm bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"
      ><i class="fa-regular fa-compass"></i
    ></a>
    <a class="navbar-brand eliteStay" href="/listings">EliteStay </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a id="explore" class="nav-link" aria-current="page" href="/listings"
          >Explore</a
        >
      </div>

      <div class="navbar-nav ms-auto">
        <form method="POST" action="/listings/destination" class="d-flex" role="search">
          <input
            class="form-control me-2 search-inp"
            type="search"
            name="listing[location]"
            placeholder="Search destinations"
          />
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>Search
          </button>
        </form>
      </div>

      <div class="navbar-nav ms-auto">
        <a id="newForm" class="nav-link" href="/listings/new">Become a host</a>
        <% if(!currUser){%>
        <a id="newForm" class="nav-link" aria-current="page" href="/signup"
          ><b>Sign Up</b></a
        >
        <a id="newForm" class="nav-link" href="/login"><b>Log in</b></a>
        <%}%> <% if(currUser){%>
        <a class="nav-link" href="/logout"><b>Log Out</b></a>
        <%}%>
      </div>
    </div>
  </div>
</nav>
